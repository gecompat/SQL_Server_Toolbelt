# NICHT AUSFÜHRBAR – Nur Vorlage / NOT EXECUTABLE – Template only
# Alle Platzhalter ersetzen und das Manifest gegen den tatsächlichen Modulstand prüfen.

module_id: "{{ModuleId}}"
module_name: "{{ModuleName}}"
version: "0.0.0"
status: proposed
description: "{{Kurze fachliche Beschreibung}}"

sql_server_versions:
  - "2019"
  - "2022"
  - "2025"

compatibility_levels: []

platforms:
  windows: "not executed"
  linux: "not executed"

providers:
  - id: default
    technology: T-SQL
    platforms: [Windows, Linux]
    status: "not executed"

deployment_capabilities:
  local: true
  central: true
  central_with_synonyms: false
  local_required: false
  central_preferred: false

dependencies: []
# Beispiel:
# dependencies:
#   - module_id: "toolbelt_core"
#     min_version: "1.0.0"
#     location: same_database

schemas:
  - "toolbelt_{{category}}"

objects: []
# Beispiel:
# objects:
#   - type: USP
#     schema: "toolbelt_{{category}}"
#     name: "USP_{{ObjectName}}"
#     visibility: public

permissions_required: []
collation_contract: "{{Caller oder invariant semantics}}"
data_type_contract: "{{varchar/nvarchar und weitere relevante Typentscheidungen}}"
help_contract_version: "1.0"
resultset_contracts: []

clr:
  used: false
  permission_set: null
  trust_method: null

lifecycle:
  install: "Deployment/Install.sql"
  upgrade: "Deployment/Upgrade.sql"
  uninstall: "Deployment/Uninstall.sql"

tests:
  static: "Tests/Static/"
  runtime: "Tests/Runtime/"
  contract: "Tests/Runtime/"

validation_status: "not executed"
