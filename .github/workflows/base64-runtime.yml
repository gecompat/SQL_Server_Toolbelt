name: Base64 Runtime

on:
  pull_request:
    paths:
      - ".github/workflows/base64-runtime.yml"
      - "Modules/toolbelt.conversion.base64/module.yaml"
      - "Modules/toolbelt.conversion.base64/Source/**"
      - "Modules/toolbelt.conversion.base64/Deployment/**"
      - "Modules/toolbelt.conversion.base64/Tests/Runtime/**"
      - "Modules/toolbelt.conversion.base64/Tests/Static/**"
      - "Tests/CI/run-base64-linux.sh"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/base64-runtime.yml"
      - "Modules/toolbelt.conversion.base64/module.yaml"
      - "Modules/toolbelt.conversion.base64/Source/**"
      - "Modules/toolbelt.conversion.base64/Deployment/**"
      - "Modules/toolbelt.conversion.base64/Tests/Runtime/**"
      - "Modules/toolbelt.conversion.base64/Tests/Static/**"
      - "Tests/CI/run-base64-linux.sh"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: base64-runtime-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sql-server-2025-linux:
    name: SQL Server 2025 Linux
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate static contract
        run: >-
          python3
          Modules/toolbelt.conversion.base64/Tests/Static/validate_contract.py

      - name: Execute synthetic compatibility and lifecycle tests
        env:
          TBX_SQL_IMAGE: mcr.microsoft.com/mssql/server:2025-latest
        run: bash Tests/CI/run-base64-linux.sh
