name: Generate-Series Runtime

on:
  pull_request:
    paths:
      - ".github/workflows/generate-series-runtime.yml"
      - "Modules/toolbelt.core.generate-series/module.yaml"
      - "Modules/toolbelt.core.generate-series/Source/**"
      - "Modules/toolbelt.core.generate-series/Deployment/**"
      - "Modules/toolbelt.core.generate-series/Tests/Runtime/**"
      - "Modules/toolbelt.core.generate-series/Tests/Static/**"
      - "Tests/CI/run-generate-series-linux.sh"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/generate-series-runtime.yml"
      - "Modules/toolbelt.core.generate-series/module.yaml"
      - "Modules/toolbelt.core.generate-series/Source/**"
      - "Modules/toolbelt.core.generate-series/Deployment/**"
      - "Modules/toolbelt.core.generate-series/Tests/Runtime/**"
      - "Modules/toolbelt.core.generate-series/Tests/Static/**"
      - "Tests/CI/run-generate-series-linux.sh"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: generate-series-runtime-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sql-server-2025-linux:
    name: SQL Server 2025 Linux
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate static contract
        run: >-
          python3
          Modules/toolbelt.core.generate-series/Tests/Static/validate_contract.py

      - name: Execute synthetic compatibility and lifecycle tests
        env:
          TBX_SQL_IMAGE: mcr.microsoft.com/mssql/server:2025-latest
        run: bash Tests/CI/run-lab-target.sh Tests/CI/run-generate-series-linux.sh
