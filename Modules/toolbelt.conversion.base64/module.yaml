module_id: "toolbelt.conversion.base64"
module_name: "Base64 and Base64URL Conversion"
version: "1.0.0"
implementation_status: implemented
validation_status: "partially validated"
release_status: unreleased
description: "Codiert varbinary(max) als Base64 oder Base64URL und decodiert beide RFC-4648-Alphabete."

sql_server_versions:
  - "2019"
  - "2022"
  - "2025"

compatibility_levels:
  - 150
  - 160
  - 170

platforms:
  windows: "not executed"
  linux: "partially validated"

providers:
  - id: tsql-xml
    technology: T-SQL/XML
    platforms: [Windows, Linux]
    status: implemented

deployment_capabilities:
  local: true
  central: true
  central_with_synonyms: false
  local_required: false
  central_preferred: false

dependencies: []

schemas:
  - "toolbelt_conversion"

objects:
  - type: SVF
    schema: "toolbelt_conversion"
    name: "SVF_Base64Encode"
    visibility: public
  - type: SVF
    schema: "toolbelt_conversion"
    name: "SVF_Base64Decode"
    visibility: public

permissions_required:
  - "SELECT oder REFERENCES auf den Scalar-valued Functions"
  - "DDL-Rechte für Deploy und Uninstall"

collation_contract: "Base64 ist ein ASCII-Vertrag; der Encode-Rückgabewert verwendet die Datenbank-Default-Collation."
data_type_contract: "Encode akzeptiert ausschließlich varbinary(max); Decode ausschließlich varchar(max); NULL bleibt NULL; keine implizite Zeichenkodierung."
help_contract_version: "not applicable"
resultset_contracts: []

lifecycle_error_range: "51040-51049"

clr:
  used: false
  permission_set: null
  trust_method: null

lifecycle:
  deploy: "Deployment/Deploy.sql"
  uninstall: "Deployment/Uninstall.sql"
  execution_mode: "SQLCMD"
  release_manifest: "embedded"
  source_hash_role: "diagnostic_only"

documentation:
  module: "README.md"
  public_objects:
    - "Documentation/SVF_Base64Encode.md"
    - "Documentation/SVF_Base64Decode.md"
  architecture:
    - "../../Documentation/Architecture/BASE64_MODULE_DESIGN.md"
  test_matrix:
    - "Tests/BASE64_CONTRACT_TEST_MATRIX.md"
  evidence:
    - "Tests/README.md"

contracts:
  usp: "not applicable"
  deployment: "1.0"
  validation: "1.0"

tests:
  static: "Tests/Static/validate_contract.py"
  runtime: "Tests/Runtime/Base64.Contract.sql"
  contract: "Tests/Runtime/Base64.Contract.sql"
  lifecycle: "Tests/Runtime/Lifecycle.Contract.sql"
  central: "Tests/Runtime/Central.Contract.sql"
  github_hosted_linux: "../../.github/workflows/base64-runtime.yml"

validation_evidence:
  - date: "2026-07-29"
    workflow: "https://github.com/gecompat/SQL_Server_Toolbelt/actions/workflows/base64-runtime.yml"
    scope: "Workflow und Contract-Tests vorhanden; Runtime noch nicht ausgeführt"
    result: "not executed"
  - date: "2026-07-29"
    workflow: "https://github.com/gecompat/SQL_Server_Toolbelt/actions/runs/30493304673"
    scope: "SQL Server 2025 Linux; Compatibility Levels 150, 160 und 170; RFC-4648-, Fehler-, Größen-, lokale, zentrale und Lifecycle-Contracts"
    result: "success"
