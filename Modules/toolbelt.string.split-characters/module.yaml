module_id: "toolbelt.string.split-characters"
module_name: "Literal Multi-Separator Split"
version: "1.0.0"
implementation_status: implemented
validation_status: "not executed"
release_status: unreleased
description: "Teilt Unicode-Text portabel an einer Menge einzelner literal interpretierter Separatorzeichen."

sql_server_versions:
  - "2019"
  - "2022"
  - "2025"

compatibility_levels:
  - 150
  - 160
  - 170

platforms:
  windows: "not executed"
  linux: "not executed"

providers:
  - id: tsql-inline
    technology: T-SQL Inline TVF
    platforms: [Windows, Linux]
    status: implemented

deployment_capabilities:
  local: true
  central: true
  central_with_synonyms: false
  local_required: false
  central_preferred: false

dependencies:
  - module_id: "toolbelt.core.generate-series"
    minimum_version: "1.0.0"
    objects:
      - "toolbelt_core.TVF_GenerateSeriesBigInt"

schemas:
  - "toolbelt_string"

objects:
  - type: TVF
    schema: "toolbelt_string"
    name: "TVF_SplitByCharacters"
    visibility: public

permissions_required:
  - "SELECT oder REFERENCES auf den Inline Table-valued Functions"
  - "DDL-Rechte für Deploy und Uninstall"

collation_contract: "Separatoren werden als einzelne UTF-16-Codeeinheiten mit Latin1_General_100_BIN2 verglichen; Groß-/Kleinschreibung und Akzente werden nicht gleichgesetzt."
data_type_contract: "Input und Value sind nvarchar(max); Separators ist nvarchar(4000); Ordinal ist bigint und beginnt bei 1."
help_contract_version: "not applicable"
resultset_contracts:
  - name: "TVF_SplitByCharacters"
    columns:
      - "Value nvarchar(max)"
      - "Ordinal bigint"

lifecycle_error_range: "51070-51079"

clr:
  used: false
  permission_set: null
  trust_method: null

lifecycle:
  deploy: "Deployment/Deploy.sql"
  uninstall: "Deployment/Uninstall.sql"
  execution_mode: "SQLCMD"
  release_manifest: "embedded"
  source_hash_role: "diagnostic_only"

documentation:
  module: "README.md"
  public_objects:
    - "Documentation/TVF_SplitByCharacters.md"
  architecture:
    - "../../Documentation/Architecture/SPLIT_CHARACTERS_MODULE_DESIGN.md"
  test_matrix:
    - "Tests/SPLIT_CHARACTERS_CONTRACT_TEST_MATRIX.md"
  evidence:
    - "Tests/README.md"

contracts:
  usp: "not applicable"
  deployment: "1.0"
  validation: "1.0"

tests:
  static: "Tests/Static/validate_contract.py"
  runtime: "Tests/Runtime/SplitCharacters.Contract.sql"
  contract: "Tests/Runtime/SplitCharacters.Contract.sql"
  lifecycle: "Tests/Runtime/Lifecycle.Contract.sql"
  central: "Tests/Runtime/Central.Contract.sql"
  github_hosted_linux: "../../.github/workflows/split-characters-runtime.yml"

validation_evidence:
  - date: "2026-07-30"
    workflow: "https://github.com/gecompat/SQL_Server_Toolbelt/actions/workflows/split-characters-runtime.yml"
    scope: "Workflow und Contract-Tests vorhanden; Runtime noch nicht ausgeführt"
    result: "not executed"
