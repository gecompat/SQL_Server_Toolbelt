/* Manual Windows-only test entry point.
   The operator supplies a synthetic configured RootAlias at execution time.
   No runtime paths, identities or output are committed to the repository. */
SET NOCOUNT ON;
GO
